{
  "name": "k-cuatrobet-com",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:repo": "npm run build && node ../../../template/scripts/build-repo.mjs",
    "build:server": "npm run build && node ../../../template/scripts/build-server.mjs"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "modern-normalize": "^3.0.1"
  }
}
